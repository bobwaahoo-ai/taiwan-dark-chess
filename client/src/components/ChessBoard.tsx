import {GameState,chars} from "../types/game";export function ChessBoard({game,me,selected,onCellClick,legal}:{game:GameState;me:string;selected:number|null;onCellClick:(i:number)=>void;legal:Set<number>}){return <div className="board">{game.board.map((p,i)=>{const mine=!!p?.revealed&&p.color===game.players.find(x=>x.id===me)?.color;return <button key={i} className={`cell ${p?.revealed?"revealed":"hidden"} ${selected===i?"selected":""} ${legal.has(i)?"legal":""}`} onClick={()=>onCellClick(i)}><span>{p?.revealed?chars[p.type!]:"暗"}</span></button>})}</div>}